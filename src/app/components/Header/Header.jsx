import CityChangeInput from "../CityChangeInput/CityChangeInput";
import CityData from "../CityData/CityData";

const Header = () => {
  return (
    <header>
      <CityChangeInput />
      <CityData />
    </header>
    
  );
};

export default Header;
